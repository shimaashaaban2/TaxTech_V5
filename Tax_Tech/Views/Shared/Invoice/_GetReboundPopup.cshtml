@*@using Tax_Tech.Repository;
@{
    InvoiceApiRepository _invoiceApiRepository = new InvoiceApiRepository();
    List<Tax_Tech.ViewModels.BranchViewModel> branches = ViewBag.Branches;
    List<Tax_Tech.ViewModels.VendorViewModel> vendors = ViewBag.Vendors;
}*@

<div class="modal fade text-left" id="default" data-backdrop="static" tabindex="-1" role="dialog" aria-labelledby="myModalLabel1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title" id="myModalLabel1">Rebound Docs</h4>
                <button type="button" class="close" data-dismiss="modal" onclick="DismissPopup()" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <!-- Create New Form Controls -->
            <div class="modal-body">

                <div class="row">
                    <div class="form-group col-md-6">
                        <label class="control-label" for="StartDate">
                            Start Date

                        </label>
                        <input tabindex="2" value="@DateTime.Now.AddDays(-7).ToString("yyyy-MM-dd")" type="text" id="" name="InvoiceIssueDate" class="form-control flatpickr-date-time flatpickr-input active" placeholder="YYYY-MM-DD" readonly="readonly">
                    </div>
                    <div class="form-group col-md-6 mt-2">
                        <button type="submit" class="btn btn-primary" id="Rebound-btn" tabindex="18">Submit</button>
                        <button id="btn-create-loader" class="btn btn-outline-primary waves-effect" type="button" disabled="" style="display: none;">
                            <span class="spinner-border spinner-border-sm" role="status" aria-hidden="true"></span>
                            <span class="ml-25 align-middle">Loading...</span>
                        </button>
                    </div>
                </div>
                <div class="row">
                    
                </div>
               
                @*<div class="card-footer">
            <button type="submit" class="btn btn-primary" id="Rebound-btn" tabindex="18">Save</button>
            <button id="Rebound-Btn-loader" class="btn btn-outline-primary waves-effect hidden btn-load" type="button" disabled="">
                <span class="spinner-border spinner-border-sm" role="status" aria-hidden="true"></span>
                <span class="ml-25 align-middle">@Tax_Tech.Resources.Resource.Loading...</span>
            </button>
        </div>*@


            </div>
        </div>
    </div>
</div>

<script>
    $('.select2').each(function () {
        var $this = $(this);
        $this.wrap('<div class="position-relative"></div>');
        $this.select2({
            dropdownAutoWidth: true,
            width: '100%',
            dropdownParent: $this.parent()
        });
    });
    var dateTimePickr = $('.flatpickr-date-time');
    if (dateTimePickr.length) {
        dateTimePickr.flatpickr({
            enableTime: true
        });
    }
</script>